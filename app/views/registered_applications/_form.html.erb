<div class="container main-content">
<%= form_with(model: registered_application, local: true) do |form| %>
  <% if registered_application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(registered_application.errors.count, "error") %> prohibited this registered_application from being saved:</h2>

      <ul>
      <% registered_application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :registered_application_name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :registered_application_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
</div>
